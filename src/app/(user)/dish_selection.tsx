import React from 'react';

// this is the file acts as the route to reach the dish selection page

// Import my DishSelectionScreen from its actual location
import DishSelectionScreen from '../../screens/user/DishSelectionScreen'; // Adjust this path if necessary

export default function Page() {
  return <DishSelectionScreen />;
}
